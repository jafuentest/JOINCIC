<%= form_for(@premio) do |f| %>
  <% if @premio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@premio.errors.count, "error") %> prohibited this premio from being saved:</h2>

      <ul>
      <% @premio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :rifa_id %><br />
    <%= f.number_field :rifa_id %>
  </div>
  <div class="field">
    <%= f.label :patrocinante_id %><br />
    <%= f.number_field :patrocinante_id %>
  </div>
  <div class="field">
    <%= f.label :participante_id %><br />
    <%= f.number_field :participante_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
