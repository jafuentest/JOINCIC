<%= form_for(@material_pop) do |f| %>
  <% if @material_pop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@material_pop.errors.count, "error") %> prohibited this material_pop from being saved:</h2>

      <ul>
      <% @material_pop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="formularioS">
    <tr>
      <th></th>
      <th></th>
    </tr>
    <tr>
      <td>
        <div class="field">
          <%= f.label :nombre %>
          <%= f.text_field :nombre, :size => 6, :maxlength => 1 %>
        </div>
      </td>
    </tr>
    <tr>
      <td>
        <div class="field">
          <%= f.label :cantidad %>
          <%= f.number_field :cantidad, :in => 1..999 %>
        </div>
      </td>
    </tr>
  </table>
  
  <br />
  
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>