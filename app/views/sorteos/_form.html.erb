<%= form_for(@sorteo) do |f| %>
  <% if @sorteo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sorteo.errors.count, "error") %> prohibited this sorteo from being saved:</h2>

      <ul>
      <% @sorteo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_par %><br />
    <%= f.number_field :id_par %>
  </div>
  <div class="field">
    <%= f.label :id_premio %><br />
    <%= f.number_field :id_premio %>
  </div>
  <div class="field">
    <%= f.label :entregado %><br />
    <%= f.check_box :entregado %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
